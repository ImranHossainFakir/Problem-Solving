
// Definin 2D vector
// N = number of rows
// {INF, INF} representing a vector of size 2, 
// and we re initializing each row with this vector as the initial value.
vector<vector<long long>> min_cost(N, {INF, INF});


